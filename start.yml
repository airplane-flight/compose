services:
  flame:
    image: pawelmalak/flame:latest
    container_name: start
    restart: unless-stopped
    environment:
      - PASSWORD=${PASS}
    ports:
      - 5005:5005
    volumes:
      - /home/amccue/.config/docker/flame:/app/data
